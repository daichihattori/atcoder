#include <bits/stdc++.h>
using namespace std;

int main(){
    // //vector int input
    // vector <int> integers={};
    // int buf_int;
    // while(cin>>buf_int){
    //     integers.push_back(buf_int);
    // }

    // //vector int output
    // for(int i=0;i<integers.size();i++){
    //     cout << integers.at(i);
    // }
    // cout << endl;

    //vector string input
    vector <string> strings={};
    string buf_string;
    while(cin>>buf_string){
        strings.push_back(buf_string);
    }

    //vector string output
    for(int i=0;i<strings.size();i++){
        cout << strings.at(i);
    }
    cout << endl;


}